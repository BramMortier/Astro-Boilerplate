---
interface Props  {
    Tag?: "p" | "strong" | "i";
    text: string;
    class?: string;
}

const { text, Tag = "p", class: className } = Astro.props;
---

<Tag class:list={className}>
    {text}
</Tag>